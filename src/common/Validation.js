export const Validation = (listRegex) => {
  // console.log(listRegex[0].nameInput.pattern);
  // listRegex.map((info) => {
  //   let result = info.nameInput.pattern.test(info.nameInput.inputValue.trim());
  //   if (info.input === "email" && info.input.trim() === "") {
  //     info.error = info.messageError1;
  //   } else {
  //     if (result) {
  //       info.error = "";
  //     } else {
  //       info.error = info.messageError;
  //     }
  //   }
  // });
};
